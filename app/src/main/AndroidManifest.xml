<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_prototype"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_prototype_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudyProject3">
        <activity
            android:name=".ControllerActivity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ControllerActivity.DetailTaskActivity"
            android:exported="false"
            android:label="Подробнее о задании"/>
        <activity
            android:name=".ControllerActivity.OneTaskActivity"
            android:exported="false"
            android:label="1 Задание"/>
    </application>

</manifest>